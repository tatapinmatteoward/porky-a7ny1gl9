---
const currentYear = new Date().getFullYear();
const socialLinks = [
  { icon: 'fab fa-twitter', url: '#' },
  { icon: 'fab fa-facebook', url: '#' },
  { icon: 'fab fa-instagram', url: '#' },
  { icon: 'fab fa-pinterest', url: '#' }
];
---

<footer class="bg-[#4b0026] text-[#808000] py-8 border-t border-[#808000]">
  <div class="container mx-auto px-4 text-center">
    <p class="text-sm">&copy; {currentYear} Porky Pie Tales. All rights reserved.</p>
    <div class="flex justify-center gap-4 mt-4">
      {socialLinks.map((link) => (
        <a href={link.url} class="hover:text-white transition-colors duration-300">
          <i class={link.icon}></i>
        </a>
      ))}
    </div>
  </div>
</footer>